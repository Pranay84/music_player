const EachCard = (props) => {
  const {
    id,
    status,
    sort,
    user_created,
    date_created,
    user_updated,
    date_updated,
    name,
    artist,
    accent,
    cover,
    top_track,
    url,
  } = props;

  console.log(id);
};

export default EachCard;
